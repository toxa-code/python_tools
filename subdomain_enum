#Subdomain Enumeration with Python
#on Linux use "pyinstaller" , convert an Windows exe with "py2exe"

import requests 
import sys 

sub_list = open("subdomains.txt").read()    #the subdomains.txt is a wordlist that need to be in the same directory#
subdoms = sub_list.splitlines()

for sub in subdoms:
    sub_domains = f"http://{sub}.{sys.argv[1]}"     #if use path, [2]

    try:
        requests.get(sub_domains)
    
    except requests.ConnectionError: 
        pass
    
    else:
        print("Valid domain: ",sub_domains)   
        
